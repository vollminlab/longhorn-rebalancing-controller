apiVersion: v2
name: longhorn-rebalancing-controller
description: Size-aware Longhorn replica rebalancer — surge-moves over-placed replicas (add on destination, wait healthy, remove source)
type: application
version: 0.3.1
appVersion: "0.3.1"
