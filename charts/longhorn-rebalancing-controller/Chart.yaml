apiVersion: v2
name: longhorn-rebalancing-controller
description: Size-aware Longhorn replica rebalancer — evicts over-placed replicas so Longhorn rebuilds them on less-loaded nodes
type: application
version: 0.1.2
appVersion: "0.1.2"
